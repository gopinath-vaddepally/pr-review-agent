# Minimal requirements for simple mode (no LangChain, no database)

# Core web framework
fastapi==0.115.0
uvicorn[standard]==0.30.0
python-multipart==0.0.9

# Azure DevOps SDK
azure-devops==7.1.0b4

# AI - Multiple options supported:
# 1. Groq (free, fast) - Get key from https://console.groq.com/
# 2. Anthropic Claude - Get key from https://console.anthropic.com/ ($5 free)
# 3. Ollama (local only) - Install from https://ollama.com/
anthropic==0.39.0  # Optional: only if using Anthropic
groq==0.11.0  # Optional: only if using Groq (recommended for free cloud deployment)

# Environment and configuration
python-dotenv==1.0.1
pydantic==2.9.0
pydantic-settings==2.5.0

# Utilities
httpx==0.27.0
pyyaml==6.0.2
